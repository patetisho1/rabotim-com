# 📊 Как да провериш GitHub Actions тестовете

## 🔗 Директен линк:
**https://github.com/patetisho1/rabotim-com/actions**

---

## 📋 Какво да провериш:

### 1. Най-новият Workflow Run
- Кликни на най-новия workflow (най-горе в списъка)
- Трябва да видиш статус:
  - ✅ **Зелен кръг** = Успешен
  - ❌ **Червен X** = Провален
  - 🟡 **Жълт кръг** = В процес
  - ⏸️ **Сива линия** = Отменен

### 2. Детайли на Test Job
Кликни на **"test"** job и провери:

#### ✅ Ако е успешен:
- Всички тестове са минали ✅
- Ще видиш green checkmarks за всеки тест файл

#### ❌ Ако е провален:
**Провери следното:**

**a) Build проблеми:**
- Потърси `npm ci` или `npm install`
- Провери за dependency errors

**b) Web Server проблеми:**
- Потърси `npm run dev` или `webServer`
- Провери за `Cannot find module` грешки
- Провери за `Timed out waiting` грешки

**c) Test failures:**
- Потърси `Playwright tests` секция
- Виж кои тестове са се провалили
- Потърси `test-results.json` или `test-analysis-report.txt`

**d) Environment Variables:**
- Провери дали `TEST_USER_EMAIL` и `TEST_USER_PASSWORD` са настроени в GitHub Secrets
- Провери дали `PLAYWRIGHT_TEST_BASE_URL` е настроен (опционално)

### 3. Artifacts (Ако има провалили тестове)
- Скрол до долу
- Намери секция **"Artifacts"**
- Download `playwright-report` за детайлен HTML отчет
- Download `test-results.json` за структурирани данни
- Download `test-analysis-report.txt` за AI анализ

### 4. Логове
- Кликни на конкретния failed тест
- Скрол надолу за да видиш детайлни логове
- Провери за stack traces и error messages

---

## 🔧 Често срещани проблеми:

### 1. Web Server не стартира
**Причина:** Build проблем или missing dependencies
**Решение:** Провери build логовете

### 2. Environment Variables липсват
**Причина:** `TEST_USER_EMAIL` или `TEST_USER_PASSWORD` не са настроени
**Решение:** Добави ги в GitHub Secrets:
- Settings → Secrets and variables → Actions → New repository secret

### 3. Timeout грешки
**Причина:** Тестовете отнемат твърде много време
**Решение:** Увеличи timeout в `playwright.config.ts`

### 4. Module not found
**Причина:** Dependency проблем или cache issue
**Решение:** Провери дали `npm ci` е успешен

---

## 📊 Резултати от последния commit:

**Commit:** `36187b5` - Fix build errors: Add dynamic exports to API routes

**Очаквано:**
- ✅ Build трябва да мине (вече минава локално)
- ⚠️ Тестовете може да се провалят ако липсват environment variables
- ⚠️ Web server може да има проблеми ако има dependency issues

---

## 🔗 Полезни линкове:

- **Workflow файл:** `.github/workflows/playwright.yml`
- **Test конфигурация:** `playwright.config.ts`
- **Test файлове:** `tests/e2e/`

---

## 📝 Следващи стъпки:

1. Провери GitHub Actions страницата
2. Ако има провалили тестове - виж логовете
3. Ако липсват environment variables - добави ги в Secrets
4. Ако има build проблеми - провери dependency errors
5. Download artifacts за детайлни резултати


