# 🔧 Възстановяване на Проектните Настройки

**Цел:** Пълен списък с всички настройки за възстановяване след преинсталация

---

## ✅ Какво Е Достатъчно в Git

### ✅ ДОСТАТЪЧНО (в git):
1. **package.json** - Всички зависимости и версии
2. **next.config.js** - Next.js конфигурация
3. **tailwind.config.js** - Tailwind CSS настройки
4. **tsconfig.json** - TypeScript конфигурация
5. **playwright.config.ts** - Playwright конфигурация
6. **.github/workflows/** - CI/CD workflows
7. **Структура на проекта** - Всички файлове

### ⚠️ НЕ Е В GIT (трябва да се създаде):
1. **`.env.local`** - Environment variables (секрети)
2. **`.vscode/settings.json`** - Cursor/VS Code настройки (ако искаш)
3. **`node_modules/`** - Dependencies (инсталират се с `npm install`)

---

## 📋 Пълна Checklist за Възстановяване

### Стъпка 1: Clone проекта
```powershell
git clone https://github.com/patetisho1/rabotim-com.git
cd rabotim-com
git checkout staging
```

✅ Резултат: Всички файлове са на места

---

### Стъпка 2: Инсталирай зависимости
```powershell
npm install
```

✅ Резултат: Всички пакети са инсталирани (package.json се чете автоматично)

---

### Стъпка 3: Създай `.env.local`

**Опция 1: От env.example**
```powershell
copy env.example .env.local
code .env.local  # Попълни стойностите
```

**Опция 2: Ръчно**
```powershell
# Създай .env.local файл
New-Item -Path .env.local -ItemType File
code .env.local
```

**Попълни:**
```bash
NEXT_PUBLIC_SUPABASE_URL=https://wwbxzkbilklullziiogr.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=тук_твоят_anon_key
SUPABASE_SERVICE_ROLE_KEY=тук_твоят_service_role_key
```

✅ Резултат: Environment variables са настроени

**Как да намериш keys:**
1. Отиди в Supabase Dashboard: https://supabase.com/dashboard
2. Избери проекта: `rabotim-com`
3. Settings → API
4. Копирай URL и keys

---

### Стъпка 4: Провери конфигурациите

**package.json:**
- ✅ Версии на пакетите са правилни
- ✅ Scripts са правилни

**next.config.js:**
- ✅ Supabase image domain е правилен
- ✅ Security headers са правилни

**tailwind.config.js:**
- ✅ Custom colors са правилни
- ✅ Dark mode е enabled

✅ Резултат: Всички конфигурации са правилни

---

### Стъпка 5: Тествай Build

```powershell
npm run build
```

✅ Резултат: Build минава успешно

---

### Стъпка 6: Стартирай Dev Server

```powershell
npm run dev
```

✅ Резултат: Dev server работи на http://localhost:3000

---

### Стъпка 7: Възстанови Cursor IDE Настройки (Опционално)

Създай `.vscode/settings.json`:
```json
{
  "editor.fontSize": 14,
  "editor.tabSize": 2,
  "editor.formatOnSave": true,
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000,
  "terminal.integrated.defaultProfile.windows": "PowerShell",
  "typescript.tsdk": "node_modules/typescript/lib",
  "typescript.enablePromptUseWorkspaceTsdk": true
}
```

✅ Резултат: Cursor IDE е настроен

---

### Стъпка 8: Възстанови Chat Контекста

```powershell
.\restore-chat-context.ps1
```

Или в AI чата кажи:
```
Прочети CHAT_CONTEXT.md файла и възстанови контекста на проекта.
```

✅ Резултат: AI знае какво работихме

---

## 🎯 Отговор на Въпроса

### ❓ "Ще ли бъде достатъчен chat контекстът в GitHub?"

**✅ ОТГОВОР: ДА, но с малко допълнителни стъпки:**

1. **CHAT_CONTEXT.md** ще ти даде:
   - ✅ Какво работихме
   - ✅ Текущо състояние на проекта
   - ✅ Важни бележки

2. **Git репозиторият** ще ти даде:
   - ✅ Всички файлове
   - ✅ package.json (версии на пакетите)
   - ✅ Конфигурационни файлове (next.config.js, tailwind.config.js)
   - ✅ Структура на проекта

3. **Трябва да направиш ръчно:**
   - ⚠️ Създай `.env.local` файл с твоите Supabase keys
   - ⚠️ Инсталирай зависимости (`npm install`)
   - ⚠️ (Опционално) Настрой Cursor IDE

---

## 💡 Препоръка

**След преинсталация:**

1. Clone проекта от GitHub ✅
2. Инсталирай зависимости (`npm install`) ✅
3. Създай `.env.local` с твоите keys ⚠️
4. Възстанови chat контекста (CHAT_CONTEXT.md) ✅
5. Кажи на AI: "Прочети CHAT_CONTEXT.md и PROJECT_SETUP_RESTORE.md и възстанови настройките" ✅

**Резултат:** Всичко ще работи както преди! ✅

---

## 🔗 Полезни Файлове

- `CHAT_CONTEXT.md` - Chat контекст
- `PROJECT_SETUP_RESTORE.md` - Този файл (setup инструкции)
- `env.example` - Пример за environment variables
- `README.md` - Обща документация
- `SETUP_GUIDE.md` - Пълен setup guide

---

**Запази този файл - ще ти трябва след преинсталация!** ✅

